import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'careconnect-front-react',
  webDir: 'dist'
};

export default config;
